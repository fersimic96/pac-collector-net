namespace PacCollector.Infrastructure.Plugins.Print;

// que tipo de parser usar para un reporte print: label:value o tabla de destilacion
public enum PrintReportKind
{
    LabelValue,
    Distillation,
}
